wezterm
- fix colors to that they match colorscheme in nvim

nvim

- Roslyn: disable unnecessary printing. 
- Roslyn: Update code
- Roslyn: Remove the "No solution" alternative. Pressing Enter with no given solution should be enough and it's simpler.
- Roslyn: Remember "No solution" selection. At the moment you will be prompted for each new buffer.
- Harpun/Roslyn: Fix solution picker appearing twice when opening file from harpun.
- Check if search in files or quickfix in fzf-lua can take regex. E.g. would be nice to filter "\.UT$" in find files.
- Trim filepath in fzf-lua. 
- Have a single .editorconfig for repo.
- Add code action float at cursor.
- Add user command for building the current project in dotnet (if you're in a solution with many projects). 
- Add relative numbers in Terminal
- fix colors for fzf-lua
- Remove dependency on plenary. It's limited use can be self implemented

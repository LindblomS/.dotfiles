- Added vim.o.winborder = "rounded" and removed all other border logic for hover windows. 
- Replaced lsp signature normal keymap <leader>ls with <C-s> (the same one in insert mode).
- Set virtual_text to true in vim.diagnostic.config

todo
- migrate all lsp:s to new vim.lsp.config
- install blink

wezterm
- fix colors so that they match colorscheme in nvim

nvim

- Have a single .editorconfig for repo.
- Add code action float at cursor.
- Idea. The issue is that I regularly overwrite my mark (m-a) and can't keep track of different
  marks. The idea is to keep an appendable log of marks so I can always mark with the same key but
  still keep any "overwritten" ones. 
- Catch errors when trying to load plugins
- Add install script for eslint_ls and document how eslint works with it and everything.
- Add keymap for visual selection <leader>fW which works like normal <leader>fW, i.e. uses word
  boundary \bSomeWord\b
- Implement a fzf-like ui for selecting the yank-register. See :reg 
- Add <leader>/ to live grep in buffer
- Investigate whether to remove the built in documentation window from popping up.
